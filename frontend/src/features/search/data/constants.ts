import { SearchSchema } from "../components/Search";

export const SEARCH_TITLE: Record<keyof SearchSchema, string> = {
	"users-extended": "Users",
	"users-short": "Users",
	grades: "Classes",
	schools: "Schools",
};
